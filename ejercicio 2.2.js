// 2. Desarrollar un algoritmo que nos calcule el cuadrado de los 10 primeros números naturales.

let contador = 0;

while (contador <= 10) {

    let cuadrado = contador*contador;

    console.log(`cuadrado de ${contador} = ${cuadrado}`);

    contador++;
    
}
